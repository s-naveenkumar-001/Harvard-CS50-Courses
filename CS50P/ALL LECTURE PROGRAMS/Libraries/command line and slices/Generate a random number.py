# In this program we guess or generate a random number in between 1 to 10.
# from random library we import the randint module.
# In python documentation " random .randint(a, b) ".
# the probability for this program 10% is given equaly.

from random import randint
Number = randint(1, 10)
print(Number)