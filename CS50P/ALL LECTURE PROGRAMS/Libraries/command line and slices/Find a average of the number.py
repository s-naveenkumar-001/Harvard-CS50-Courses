# In this program we find the average of the number.
# import the statistics library.
# In python documentation statistics.mean(data). in data we enter we list of numbers.
# First we import the library and we use that to find the average of the number.

import statistics
print(statistics.mean([100, 90]))